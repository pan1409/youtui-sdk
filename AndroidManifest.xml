<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.bidaround.youtui"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_tui_108"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="YOUTUI_APPKEY"
            android:value="10000" >
        </meta-data>

        <!-- 友推渠道号，应用名(英文或拼音)+"_yt"，如：“jd_yt”，用于识别通过友推活动下载的应用，请正确填写，否则无法正确统计  -->
		<meta-data
		    android:name="YOUTUI_CHANNEL"
		    android:value="youtuidemo_yt" >
		</meta-data>

        <!-- 友推sdk meta配置，请填写您的微信appid -->
        <meta-data
            android:name="WEIXIN_APP_ID"
            android:value="wxbc9e6010cf85c3e4" >
        </meta-data>

        <!-- 友推sdk meta配置，请填写您的新浪微博appid -->
        <meta-data
            android:name="SINA_WEIBO_APP_ID"
            android:value="sina2502314449" >
        </meta-data>

        <!-- 友推sdk meta配置，请填写您的新浪微博授权代理回调地址(选填，如果已经在新浪开发者后台授权设置中将回调地址设置为友推的回调，则此处可以不填) -->
        <meta-data
            android:name="SINA_WEIBO_REDIRECT_URL"
            android:value="http://youtui.mobi/weiboResponse" >
        </meta-data>

        <activity
            android:name="cn.bidaround.youtui.YouTui"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cn.bidaround.youtui.social.SinaWeiboSSOActivity"
            android:theme="@android:style/Theme.Translucent" />
        <activity
            android:name="cn.bidaround.youtui.social.WeiXinShareActivity"
            android:theme="@android:style/Theme.Translucent" />
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent" />

        <!-- ################需要引用的外部社交jar的Activity################### -->
        <activity
            android:name="com.tencent.tauth.AuthActivity"
            android:launchMode="singleTask"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="tencent100566655" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tencent.connect.common.AssistActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name="com.tencent.plus.ImageActivity" />
    </application>

    <!-- ###################声明SDK使用的相关权限###################### -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 检测网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取mac地址作为用户的备用唯一标识 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 获取用户手机的IMEI，用来唯一的标识用户。 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 缓存资源优先存入SDcard -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许应用程序联网，以便向我们的服务器端发送数据。 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于评论时分享位置信息 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 用于评论时分享位置信息 -->

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- 用于加载图片 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 用于显示系统窗口 -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- 用于人人SSO登陆 -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <!-- 用于人人SSO登陆 -->
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <!-- 用于人人SSO登陆 -->

</manifest>